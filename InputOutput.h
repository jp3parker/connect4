#ifndef INPUT_OUTPUT_INCLUDES
#define INPUT_OUTPUT_INCLUDES

#include <iostream>
#include "data.h"
#include "game.h"

void CallBackFunc(int, int, int, int, void*);
void newImage(int, int, GameData*);
void fadedImage(int, int, GameData*);

#endif //INPUT_OUTPUT_INCLUDES
